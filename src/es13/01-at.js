// El método .at(number) del objeto no primitivo Array devuelve el valor del índice (posición) pasado como argumento.

const array = ['one', 'two', 'three', 'four', 'five', 'six'];

console.log(array[array.length - 1]);

console.log(array.at(-1));